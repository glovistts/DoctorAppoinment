spring:
  application:
    name: Doctor appointment
  datasource:
    url: jdbc:postgresql://host:5437/port
    username: username
    password: password
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: doctor_schema
server:
  port: 8030

apiPrefix:
  v1: /api/v1